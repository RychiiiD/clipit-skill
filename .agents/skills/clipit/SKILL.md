---
name: clipit
description: clipit — semantic topic-based video rough-cut tool. Say a topic, AI understands the content, auto-trims off-topic segments, and produces a coherent short video.
license: MIT
---

说一句话就开始：

```text
帮我粗剪这个视频，主题是远程求职技巧
```

## 触发关键词

当你需要粗剪、剪辑、裁剪视频、切视频、剪掉废话、去掉跑题、去掉停顿、视频切片、切片导出时，使用此技能。

## 总体原则

1. **纯自然语言交互** — 用户不敲任何命令，只说一句话
2. **clipit core 只做本地音视频处理** — 语义分析由 LLM 自身完成
3. **预览确认** — 执行裁剪前必须展示给用户确认
4. **跨平台设计** — 不假设任何特定 Agent 平台或操作系统

## 安装流程

用户说类似"帮我装一下 clipit"时触发：

```text
1. 检测 Python 是否可用 → 如不可用则报错引导
2. pip install clipit（或本地 pip install -e .）
3. 调 clipit check 检查环境
4. 检查输出中的 clipit_cmd.available 和 warnings 字段：
   - available=false → 按 suggestion 将 Python Scripts 目录加入 PATH
   - 有 ffmpeg warning → 调 clipit install
5. 再次调 clipit check 确认所有项正常
6. 告知用户安装完成，可开始使用
```

**不配置任何 API Key** — 语义分析用 LLM 自带能力。

## 文件目录约定

所有过程文件和输出统一放在当前工作目录下的 `data/` 中：

```
data/
├── process-data/          ← 中间文件
│   ├── transcript.json         ← Step 2 转字幕结果（原始）
│   ├── transcript_clean.json   ← Step 2.5 清洗后字幕
│   └── decisions.json          ← Step 3 AI 裁剪决策
└── output/
    └── <原文件名>_output.mp4  ← Step 5 最终成品
```

目录自动创建，不需用户手动操作。

## 粗剪管线

### Step 1 — 意图提取

从用户 NL 中提取：
- `video_path` — 视频文件路径（用户拖入或输入）
- `topic` — 主题描述
- `clip_intensity` — 剪辑强度（默认 medium，用户可说"剪严格一点"调整）

未提供视频路径 → 请用户拖入或输入路径。
未提供主题 → 请用户描述视频主题。

### Step 2 — 转字幕

```bash
mkdir -p data/process-data
clipit transcribe <video_path> --model small -o data/process-data/transcript.json
```

读取 `data/process-data/transcript.json` 中的 `segments` 列表，向用户展示概览：
- 总时长
- 总句数
- 检测到的语言

### Step 2a — 转录确认 ⭐

**必须步骤：** 用转录内容的前几句确认该转录属于当前视频文件，防止 `data/process-data/` 下旧文件混淆。

```text
# 读取 transcript 前 3 个非空 segment 展示给用户确认
"大家好欢迎来到今天的分享" → "今天我们来聊聊自媒体的核心逻辑"
→ 用户确认："是这个视频的"
```

校验方法（二选一）：
1. 展示前 3 句让用户目视确认
2. 比对 duration 是否接近预期视频长度

> **规则：** 不依赖已存在的 transcript.json，每次重新转写，或用 duration + 前几句内容双重校验。

### Step 2.5 — 字幕清洗

```bash
clipit clean data/process-data/transcript.json -o data/process-data/transcript_clean.json
```

对 `data/process-data/transcript.json` 中的每段字幕文本执行清洗，结果保存到 `data/process-data/transcript_clean.json`（格式与原文件一致，仅 `text` 字段变化）。

清洗规则：

| 模式 | 例子 | 清洗后 |
|------|------|--------|
| 重复词 | "就，就是"、"他，他们" | "就是"、"他们" |
| 填充词 | "嗯... 就是... 这个..." | 去掉 |
| 句子开头卡顿 | "所以... 所以我认为" | "所以我认为" |
| 相邻段首尾重复 | 段A以"所以"结尾，段B以"所以"开头 | 段结尾不变，段B开头去重 |

清洗**不修改时间戳**，只改 `text` 字段。

### Step 3 — 语义分析（口播精编）

用 `data/process-data/transcript_clean.json`（已清洗过的字幕）中的整段字幕文本构造 Prompt，让 LLM 从"写一篇完整口播稿"的角度做精编：

```text
你是一个视频口播精编助手。给定一段视频字幕，从写一篇**完整的口播稿**的角度，筛选出最能连贯表达主题的核心内容。你的输出是一篇自洽的短视频脚本，不是原始视频的子集。

## 结构性要求（必须同时满足）

### 1. 完整论证链
保留的内容必须构成完整的论述结构。一篇合格的口播需包含：
- **开头钩子（前6秒）**: 视频第一段保留内容必须是直接点明主题的陈述句，不能是自我介绍、背景铺垫或互动寒暄。未点明主题前不保留任何内容。
- **论点→论据→案例→方法论→结论**: 每个论证单元必须完整。不能只保留"论点+结论"而切掉中间的分析推导过程。
- **禁止碎片化输出**: 按"段落"粒度而非"句子"粒度做 keep/cut。同一论据/案例/推导过程中连续的句子必须合并为一个 keep 段，不能切成多个小段。keep 段数量控制在 3-8 段，每个 keep 段不少于 5s（极短金句除外）。如果输出超过 10 段说明你在逐句分类而非成文精编，必须合并。
- **结论性收尾**: 视频最后一段保留内容必须是完整的结论句，不能是反问、提问或悬空句。如果原始视频结尾是提问，往前找到最近的结论句作为收尾。

### 2. 完整案例规则
当保留任何一个案例/故事时，必须保留它的**完整叙述弧**：案例引入 → 细节展开 → 分析解读 → 结论提炼。不能只保留引入和结论而切掉中间的推导，也不能只保留案例的某一半。

**特别注意：案例开场钩子紧挨跑题内容时，不要误伤。** 案例引入句（如"我来给你们讲个真实案例"）即使后面紧跟着一段与主题无关的观众互怼/闲聊，也要保留引入句。正确做法是：保留案例开场钩子 → 裁掉跑题段 → 在后续恢复案例的展开和结论。不能因为案例的展开段被跑题打断，就连案例开场一起抛弃。

### 3. 禁止保留的内容
以下内容类型无论是否与主题沾边，一律裁掉：
- **弹幕/观众互动**: "知道的扣1不知道的扣2"、"你们觉得呢"、"把XX打在公屏上"
- **抬杠/对线**: 主播与观众争辩的内容（"你来我这儿讲"、"就显着你了"）
- **自证/卖惨**: "我98年的"、"我28岁有三家公司"等自我介绍/credential展示
- **课宣/卖课**: "下单只要199"、"想要买课的同学"、"课程限时特价"、"我的课里会讲"、"先卖个课好不好"等纯课程推销内容。**注意区分：**
  - 硬课宣（推销语气→裁掉）: "想学去拍"、"先卖个课"、"课程限时" — 删掉后前后文不受影响
  - 软提及（商业模型描述→可保留）: "我卖课有现金流"、"我做课程业务" — 属于上下文的一部分，删掉会导致逻辑断裂
- **直播间尬聊**: "今天怎么这么漂亮"、"我这么智慧的发言"等与主题无关的闲谈

### 4. 多主题处理
同一视频剪多个主题时，每个主题的粗剪结果必须是**独立完整的口播稿**。不假设观众看过其他主题。每个主题都应包含完整的开头钩子、论证结构和结论收尾。

**严格禁止跨主题保留。** 其他主题的内容即使与本主题在概念上有交叉（如"第一性原理"和"生产端思维"都涉及拆解问题），也不能保留。判断标准：这段内容直接服务于指定主题的论证链吗？如果是其他主题的论据或案例，无论多精彩都裁掉。

**主题脱节是严重错误，优先级高于保留更多内容。** 如果某段内容与当前主题的论证链没有直接关系——不是论据、不是推导、不是案例、不是结论，也不起逻辑桥梁作用——即使它本身很有道理、很精彩、时长很充裕，也必须裁掉。宁可主题内容精炼到3分钟，也不要为了凑时长塞入无关段落导致观众觉得"这是另一个话题"。

### 5. 时间顺序
splice 默认按原始时间顺序拼接。如果主题句出现在视频中段，前序内容必须在逻辑上为引出主题服务（如设问→冲突→解答的结构）。不要保留与主题无关的前序内容。

**可选：段落重排序** — 如果你认为某段金句/主题句应该在口播开头而非它在视频中的原始位置，可在 keep 段中添加 `"order": int`（从1开始）。输出时 splice 会按 order 排序而非时间顺序。两个 keep 段使用相同 order 值时按时间顺序拼接。

## 可选保留（提升内容质量）

以下类型如有空间可考虑保留，但不是必须：
- **类比/比喻**: 帮助理解的类比（如"马车 vs 汽车"的历史对比）
- **递进案例**: 从第一个案例自然引申到第二个案例（如从"造火箭"到"造新能源车也是同样思路"）
- **insight总结**: "所以你会发现真正赚到钱的人……"这类升华句

## 「内容重复」vs 递进修辞 判断标准

标记为"内容重复"时，严格区分：

- **机械重复 → "内容重复"**：同一句话几乎原样连续说两遍或多遍（如"这个很重要"紧接着"这个很重要"）。这种情况保留一句即可。
- **递进修辞 → 保留**：同一概念换说法递进情绪（如"这个方法效率很高"紧接着"这完全改变了行业规则"）。口播中这是递进，不是重复，后一句话比前一句在语义/情绪上更强或更深。

判断方法：如果后一句和前一句意思完全一样且没有增强 → 重复；如果后一句比前一句语义更强、情绪更深或换了角度深化 → 递进。

## 跨段语义重读（禁止）

多个 keep 段之间不能出现同一比喻/观点/案例的重复使用。口播精编不是"把提到相关话题的地方都选出来"，而是"用最少的内容讲完最完整的论证"。

**检查方法：**

1. **同一比喻跨段出现** — 如"摸着石头过河"在段A和段C都出现。保留最早出现或论证最完整的那段，后面的裁掉（reason="内容重复"）
2. **同一案例重复使用** — 如"双十一销售额"在段B和段D都出现作为论据。只保留第一次出现
3. **同一论点重复陈述** — 如"创业是痛苦的"在段A、段C、段F反复说。只保留最有力的那次
4. **判断标准**：如果两段 keep 在讲同一个观点/例子/比喻，无论相距多远，只保留一段。宁可少保留也要去重。

## 话题连贯性要求

相邻两个 keep 段之间的主题切换必须自然流畅。通读所有 keep 段（按时间顺序），检查相邻段之间：

- **✅ 同一主题不同方面**: "薪资太低" → "发展空间有限" → "行业在萎缩"（都是"为什么想跳槽"的不同角度，OK）
- **✅ 递进关系**: "问题是什么" → "为什么会有这个问题" → "如何解决"（逻辑递进，OK）
- **❌ 话题断裂**: "行业分析" → "午餐吃什么"（从行业分析跳到日常琐事，中间缺少逻辑连接，CUT）
- **❌ 论据跳跃**: "客户投诉流程有问题" → "前台绿植该换了"（从核心问题跳到细枝末节，论证力度下降，CUT）
- **❌ 缺少逻辑桥梁**: "传统方法行不通" → [裁掉"那还有什么其他办法"] → "新方案三步走"（从问题直接跳到方案，缺少转折桥梁，应保留桥梁内容）

判断方法：把相邻两段 keep 的文本连起来读一遍，如果感觉像是突然换了个话题在聊，则裁掉后段或补充过渡。

## 逻辑桥梁保护（防止话题断裂）

裁掉某段内容后，如果导致前后两段保留内容之间产生**话题跳跃感**（如从A话题跳到C话题，缺少B桥梁），则这段被裁的内容是必要的**逻辑桥梁**，不应裁掉。

### 什么是逻辑桥梁？

逻辑桥梁本身可能不是话题的核心论点，但它在口播结构中起到"承上启下"的作用——连接两个论证单元，让听众知道为什么要从A转向B：
- **转折句**: "但是为什么还有人能做成"（从"困难"转到"方法论"）
- **设问句**: "那具体怎么做？"（从"是什么"转到"怎么办"）
- **递进连接**: "其实远不止这样"（从"表面"转到"深层"）
- **立场声明**: "我直接说结论"（从"铺垫"转到"核心观点"）

### 判断方法

在最终输出前，按时间顺序通读所有 keep 段（跳过被裁内容）。如果相邻两段之间存在：
- 主语/话题突然更换，缺少交代 → 中间被裁的是桥梁 → 恢复保留
- 情绪倾向反转（抱怨→鼓励），缺少解释 → 中间被裁的是转折桥梁 → 恢复保留
- 从具体案例突然跳到抽象总结，缺少过渡 → 中间被裁的是推导过程 → 恢复保留

### 核心原则

> **宁可多留桥梁内容，不可让口播断裂。** 逻辑桥梁虽然不是核心论点，但它是保证口播"流畅有价值"的必要结构。缺少桥梁的 keep 段列表不是一篇口播，而是零散摘抄。

这条原则与"跨段语义重读"不冲突：桥梁内容是**结构性的**（承上启下），重复内容是**语义性的**（同一观点说两遍）。桥梁内容即使在其他段出现过类似观点也应保留，因为它的功能是连接而非陈述。

## Reason 取值规则

reason 字段必须且仅限以下 6 种之一：

- "内容保留" — 核心内容或必要铺垫
- "跑题" — 与主题完全无关、或禁止保留类型（弹幕互动、抬杠、自证等）
- "课宣" — 课程推销/带货等推广内容（硬课宣），与上下文无关的销售段落
- "例子过长" — 举例篇幅远超论证需要
- "内容重复" — 已表达过的观点反复说
- "空白停顿" — 嗯、啊、沉默等无效内容

## 输出前自检清单

在返回 JSON 之前，逐项检查：
1. [ ] 输出的第一段保留内容是否直接点明主题？（不是寒暄/自我介绍/背景）
2. [ ] 输出的最后一段是否为完整的结论句？（不是反问/提问/悬空）
3. [ ] 是否有案例被截断？（案例必须有引入+展开+分析+结论，不能只保留片段）
4. [ ] 是否有弹幕互动/抬杠/自证/课宣内容混入？
5. [ ] 保留段连起来能否读成一篇通顺的口播？（在脑中顺序朗读一遍）
6. [ ] 是否形成了"论点→论据→案例→方法论（可选）→结论"的完整结构？
7. [ ] 案例开场是否因紧挨跑题内容而被连带切掉了？案例引入句应独立保留
8. [ ] keep 段是否超过 10 段？如果超过，说明你在逐句分类，需要合并为段落级 keep
9. [ ] 是否有其他主题的内容混入？只保留当前主题的完整口播，其他主题内容一律裁掉

## 口播稿完整度评分（多视频拆分时使用）

当需要从一段长视频中提取多个独立话题时，对每个候选话题用以下 5 个维度评分，决定其是否能独立成片：

| 维度 | 满分 | 评分标准 |
|------|------|---------|
| 论点（Thesis） | 20 | 有明确的核心观点/主张，不是信息堆砌 |
| 论据（Evidence） | 20 | 有支撑论点的论证、案例、数据或推理过程 |
| 结论（Conclusion） | 20 | 有收尾总结，不是悬空句/反问结尾 |
| 逻辑递进（Flow） | 20 | 论点层层深入，前后有因果关联，不是平行堆砌 |
| 独立性（Standalone） | 20 | 脱离原视频上下文后仍能独立理解，不需要前情提要 |

**评级与决策：**
- **≥85分（≥68/80）**: ✅ 可独立成片
- **60-84分（48-67/80）**: 🔄 需要合并相邻话题或补充内容
- **<60分（<48/80）**: ❌ 不可独立成片，并入其他话题或舍弃

**规则：完整性 > 长度。** 宁愿视频多30s也要保证论点-论据-结论齐全。
10. [ ] 是否有跨段语义重读？同一比喻/观点/案例在多个 keep 段重复出现时只保留一段
11. [ ] 相邻 keep 段之间的主题切换是否通顺？读一遍，如果有"突然换话题"的感觉，需调整
12. [ ] 逐对检查相邻 keep 段——中间被裁的内容是否是必要的逻辑桥梁？如果是则恢复保留（宁可留桥不可断链）
13. [ ] 是否有与当前主题论证链无关的内容混入？即使内容本身再精彩，主题脱节也是必须修复的严重错误

用户主题：{topic}
剪辑强度：{intensity}

字幕内容（含时间戳，已清洗过重复/填充词）：
{segments}

按此格式返回 JSON：
[{"start": float, "end": float, "action": "keep"|"cut", "reason": "内容保留"|"跑题"|"课宣"|"例子过长"|"内容重复"|"空白停顿"}]

可选字段（仅对 keep 段生效）：
- "order": int — 输出顺序号（从1开始），用于非时间顺序排列段落。当你想让金句/主题句出现在开头、而背景内容在其后时使用。不填则按原始时间顺序输出。
- "topic": str — 所属话题名称（多视频拆分时使用），标记该段属于哪个话题
- "analysis": str — 口播稿完整度分析说明（多视频拆分时使用）
```

将生成的决策 JSON 保存到 `data/process-data/decisions.json`。

### Step 3.5 — 硬规则验证

LLM 不可靠，clipit 用代码强制规则修正 LLM 决策：

```bash
clipit validate data/process-data/decisions.json -i <intensity>
```

强制规则（**AI 不可覆盖**）：

| 规则 | 说明 |
|------|------|
| R1 开篇定题 | 开篇引入段强制保留（仅保护2-20s的合理开篇，超过20s的长段前摇不保护）；首段keep不超3s时连保第二段 |
| R2 短段合并 | keep 片段 < min_keep 自动合并/扩展 |
| R3 裁剪上限 | 总裁剪比例不超过 intensity 阈值，超限时恢复边缘段落 |
| R4 同动作去重 | 相邻 keep/keep 或 cut/cut 自动合并 |
| R5 Reason 校验 | reason 不在 5 种标准值内时自动标准化（保障 R3 优先级排序有效） |
| R6 流畅度防护 | keep 段过多或平均过短时裁掉孤立岛（<3s 两头切），桥接短间隙（<max_gap） |

向用户展示验证变更摘要：

```text
┌─────────────────────────────────────┐
│ 验证修正（代码强制）                  │
│ {n} 处变更                            │
│ • R1 开篇定题: [{t}-{t}] 强制保留    │
│ • R2 短段合并: [{t}-{t}] → 下一段     │
│ • R3 裁剪超限: 恢复 {n} 段            │
│ • R5 reason校验: {n} 处              │
│ • R6 流畅度修正: {n} 处              │
│                                      │
│ 保留: {n}s ({n}%)  裁剪: {n}s        │
└─────────────────────────────────────┘
```

### Step 4 — 用户确认

如果 LLM 生成的 decisions 中有 `order` 字段（段落重排序），在展示预览时额外询问用户：

```text
┌─────────────────────────────────────┐
│ 检测到段落标记了重排序，可以选择：    │
│                                      │
│ [1] 原始时间顺序（画面更自然连贯）     │
│     ↑ splice 默认，从不同时间点提取的  │
│     画面按出现顺序拼接，视觉跳变最小   │
│                                      │
│ [2] 按重排顺序输出（叙事逻辑更优）     │
│     ↑ 金句/主题句可放在开头，背景内容  │
│     放在后面，但不同时间点的画面拼接   │
│     可能有细微视觉跳跃                │
│                                      │
│ 你的选择？[1/2]                       │
└─────────────────────────────────────┘
```

用户选择 [2] 时，Step 5 的 splice 命令加 `--reorder` 参数。

展示裁剪预览给用户。**单视频模式**：

```text
┌─────────────────────────────────────┐
│ clipit — 裁剪预览                    │
│                                      │
│ 视频: {文件名}                       │
│ 主题: {topic}                       │
│ 原始: {duration}  保留: {keep_duration} ({keep_pct}%)  │
│                                      │
│ 裁剪: {cut_count}段 / 共{cut_duration}  │
│ • 跑题       {n}段 ({time})         │
│ • 课宣       {n}段 ({time})         │
│ • 例子过长  {n}段 ({time})         │
│ • 内容重复   {n}段 ({time})         │
│ • 空白停顿   {n}段 ({time})         │
│                                      │
│ 确认执行？[是/调整参数/取消]          │
└─────────────────────────────────────┘
```

**多视频拆分模式**（检测到多个 topic 时）：

```text
┌─────────────────────────────────────┐
│ clipit — 多视频拆分预览               │
│                                      │
│ 原始: 19分24秒                       │
│ 拆分: 4个独立视频                     │
│                                      │
│ 视频1 [完整度92%]: 副业赚钱方法   4:00│
│   论点→论据→结论         课宣已删    │
│ 视频2 [完整度88%]: 时间管理技巧   4:00│
│   观点→展开→升华         逻辑桥接已保│
│ 视频3 [完整度85%]: 人脉积累心法   3:55│
│   问题→方法论→结论       课宣已删    │
│ 视频4 [完整度86%]: 案例拆解      3:58│
│   案例A→案例B→结论                 │
│                                      │
│ 每个视频含独立论点-论据-结论结构       │
│                                      │
│ 确认执行？[是/调整参数/取消]          │
└─────────────────────────────────────┘
```

用户选择调整参数时，从 NL 中提取新的强度/阈值，重新走 Step 3。

### Step 5 — 执行拼接

**单视频模式：**

```bash
mkdir -p data/output
# 默认：按原始时间顺序拼接（默认）
clipit splice <video_path> -d data/process-data/decisions.json -o data/output/<原文件名>_output.mp4

# 如需段落重排：加上 --reorder（按 decisions 中 order 字段排序）
clipit splice <video_path> -d data/process-data/decisions.json --reorder -o data/output/<原文件名>_output.mp4
```

**多视频拆分模式：** 为每个视频独立生成 decisions 文件后逐个拼接。

```python
from clipit.splice import splice

for v in range(1, n+1):
    dec_path = f"data/process-data/decisions_video{v}.json"
    out_path = f"data/output/video_{v}_output.mp4"
    splice(video_path, dec_path, out_path)  # 每个视频独立的 decisions
```

> **注意：** 多视频模式下不要对一个包含所有话题的大 decisions.json 执行 splice——它会把所有 keep 段拼成一个视频，而非拆分。必须每个视频单独走 splice。

### Step 6 — 展示报告

**单视频模式：**

```text
✅ 粗剪完成！

原始: 18分32秒 → 保留: 11分15秒（省了 39%）
输出: data/output/<原文件名>_output.mp4

需要我：
[1] 再调一版   [2] 剪下一个视频
```

**多视频拆分模式：**

```text
✅ 粗剪完成！

原始: 19分24秒
输出: 4个独立视频（共占用 81.7%）

  video_1_output.mp4  — 4:00  — 副业赚钱方法  [完整度92%]
  video_2_output.mp4  — 4:00  — 时间管理技巧  [完整度88%]
  video_3_output.mp4  — 3:55  — 人脉积累心法  [完整度85%]
  video_4_output.mp4  — 3:58  — 案例拆解      [完整度86%]

课宣已清除: 3处
逻辑桥接保护: 1处
每个视频均含独立的论点-论据-结论结构

需要我：
[1] 整体再调   [2] 调整某个视频   [3] 剪下一个视频
```

## 参数调整（纯 NL 映射）

| 用户说 | 对应操作 |
|--------|---------|
| "剪严格一点" | intensity → strict，重新 Step 3 |
| "宽松点，别剪太多" | intensity → loose，重新 Step 3 |
| "保留片段别短于 3 秒" | min_keep → 3.0，重新 Step 3 |
| "片段前后留半秒过渡" | transition → 0.5，重新 Step 5 |

### Intensity 选型指引

根据目标保留比例选择 intensity：
- **loose**（max_cut_pct=50）: 保留 > 50%，宽松裁剪，仅去明显废话
- **medium**（max_cut_pct=70）: 保留 30-50%，一般粗剪（默认）
- **strict**（max_cut_pct=85）: 保留 15-30%，精编式裁剪
- **aggressive**（max_cut_pct=92）: 保留 8-15%，极度精简，仅保留最核心的口播内容

经验：当从长视频（>20min）中提取极少量精华内容时（如21分钟视频保留2-3分钟），使用 aggressive 避免 R3 恢复不需要的边缘内容。

---

## 已知限制与注意事项

### 1. validate.py 与多视频输出冲突 ⚠️

`clipit validate` 的 R4 规则会合并相邻的 keep 段。这在单视频模式下是正确的，但在**多视频拆分**模式下会破坏手动划分的视频边界（把不同视频的段合并到一起）。

| 模式 | 建议 |
|------|------|
| 单视频输出 | 正常运行 validate |
| 多视频拆分 | **跳过 validate**，或每个视频的 decisions 文件单独 validate |

### 2. 转录文件混淆风险

`data/process-data/` 目录下可能残留旧转录文件。**Step 2a（转录确认）是强制性的**，不可跳过。

### 3. 视频长度建议

| 场景 | 推荐单视频长度 |
|------|--------------|
| 短视频（抖音/小红书） | ≤ 60s |
| 中视频（B站/YouTube） | 3-5min |
| 课程切片 | 4-8min |
| 直播切片 | 3-5min |
| 多视频拆分默认 | ~4min/个 |

---

## CLI 参考（Agent 内部调用，不对用户展示）

```bash
clipit check                    # 环境检查
clipit install                  # 自动安装 ffmpeg
clipit transcribe <video>       # 转字幕，输出 JSON
clipit clean <transcript.json>  # 清洗字幕（去填充词/重复/卡顿）
clipit splice <video> -d decisions.json  # 按决策拼接
```

> **编码保护**：`transcribe` 使用 `-o` 写文件（绕过 stdout 编码问题）；所有 JSON 输出都强制 UTF-8。如遇编码问题可在命令前加 `PYTHONIOENCODING=utf-8`。<br>
> 所有命令输出 JSON，无需额外解析。
